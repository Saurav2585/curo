// Superseded by account-step.tsx + detail-form.tsx.
// Kept as an empty module because this environment can't delete files; it is
// imported by nothing and can be safely removed from the repo later.
export {};
